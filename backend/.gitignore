# Dependencies
node_modules/

# Production build outputs
dist/

# Environment files
.env
.env.local

# Database files — never commit dev database to git
prisma/*.db
prisma/*.db-journal
prisma/*.db-wal
